export const styles = {
  // keyboardAvoidingView:
  // • Layout: ocupa todo el espacio (flex-1)
  // • Color: fondo blanco (bg-white)
  keyboardAvoidingView: "flex-1 bg-white",

  // mainContainer:
  // • Layout: ocupa todo el espacio (flex-1)
  // • Tamaño: ancho completo (w-full) y ancho máximo (max-w-[25rem])
  // • Color: fondo blanco (bg-white)
  // • Alineación: centra contenido (justify-center, items-center)
  // • Espaciado: margen superior (mt-10) y padding horizontal (px-5)
  mainContainer: "flex-1 w-full max-w-[25rem] bg-white justify-center items-center px-5",

  // emailFieldContainer:
  // • Tamaño: ancho completo (w-full)
  // • Espaciado: margen inferior (mb-4)
  emailFieldContainer: "w-full mb-4",

  // emailLabel:
  // • Tipografía: fuente en negrita (font-bold)
  // • Tamaño de fuente: text-lg
  // • Color: texto negro (text-black)
  // • Alineación: texto a la izquierda (text-left)
  // • Espaciado: margen inferior (mb-1)
  emailLabel: "font-bold text-lg text-black text-left mb-1",

  // emailInputContainer:
  // • Tamaño: ancho completo (w-full)
  // • Layout: fila (flex-row) y alineación vertical (items-center)
  // • Color: fondo personalizado (bg-[#F3F4F6])
  // • Forma: bordes redondeados (rounded-xl)
  // • Posición: relativa (relative)
  emailInputContainer: "w-full flex-row items-center bg-[#F3F4F6] rounded-xl relative",

  // emailIcon:
  // • Posición: absoluta (absolute) con desplazamiento izquierdo (left-3)
  // • Orden z-index: z-20
  emailIcon: "absolute left-3 z-20",

  // emailInput:
  // • Layout: ocupa el espacio disponible (flex-1)
  // • Tamaño: altura definida (h-12)
  // • Espaciado: padding izquierdo (pl-11) y derecho (pr-12)
  // • Tipografía: tamaño (text-base) y color (text-black)
  // • Color: fondo personalizado (bg-[#F3F4F6])
  // • Forma: bordes redondeados (rounded-xl)
  // • Borde: sin borde (border-0)
  emailInput: "flex-1 h-12 pl-11 pr-12 text-base text-black bg-[#F3F4F6] rounded-xl border-0",

  // passwordFieldContainer:
  // • Tamaño: ancho completo (w-full)
  // • Espaciado: margen inferior (mb-4)
  passwordFieldContainer: "w-full mb-4",

  // passwordLabel:
  // • Tipografía: fuente en negrita (font-bold)
  // • Tamaño de fuente: text-lg
  // • Color: texto negro (text-black)
  // • Alineación: texto a la izquierda (text-left)
  // • Espaciado: margen inferior (mb-1)
  passwordLabel: "font-bold text-lg text-black text-left mb-1",

  // passwordInputContainer:
  // • Tamaño: ancho completo (w-full)
  // • Layout: fila (flex-row) y alineación vertical (items-center)
  // • Color: fondo personalizado (bg-[#F3F4F6])
  // • Forma: bordes redondeados (rounded-xl)
  // • Posición: relativa (relative)
  passwordInputContainer: "w-full flex-row items-center bg-[#F3F4F6] rounded-xl relative",

  // passwordIcon:
  // • Posición: absoluta (absolute) con desplazamiento a la izquierda (left-3)
  // • Orden z-index: z-20
  passwordIcon: "absolute left-3 z-20",

  // passwordInput:
  // • Layout: ocupa el espacio disponible (flex-1)
  // • Tamaño: altura definida (h-12)
  // • Espaciado: padding izquierdo (pl-11) y derecho (pr-12)
  // • Tipografía: tamaño (text-base) y color (text-black)
  // • Color: fondo personalizado (bg-[#F3F4F6])
  // • Forma: bordes redondeados (rounded-xl)
  // • Borde: sin borde (border-0)
  passwordInput: "flex-1 h-12 pl-11 pr-12 text-base text-black bg-[#F3F4F6] rounded-xl border-0",

  // passwordToggleButton:
  // • Posición: absoluta (absolute) con desplazamiento a la derecha (right-4)
  // • Alineación: centra contenido (items-center)
  // • Orden z-index: z-20
  passwordToggleButton: "absolute right-4 items-center z-20",

  // forgotPasswordContainer:
  // • Tamaño: ancho completo (w-full)
  // • Alineación: alinea contenido al final (items-end)
  // • Espaciado: margen inferior (mb-5)
  forgotPasswordContainer: "w-full items-end mb-5",

  // forgotPasswordText:
  // • Color del texto: personalizado (text-[#5CB868])
  forgotPasswordText: "text-[#5CB868]",

  // loginButton:
  // • Tamaño: ancho completo (w-full) y altura definida (h-12)
  // • Color: fondo personalizado (bg-[#5CB868])
  // • Alineación: centra contenido (justify-center, items-center)
  // • Forma: bordes redondeados (rounded-xl)
  // • Espaciado: margen inferior (mb-5)
  loginButton: "w-full h-12 bg-[#5CB868] justify-center items-center rounded-xl mb-5",

  // loginButtonText:
  // • Tipografía: fuente en negrita (font-bold) y tamaño (text-lg)
  // • Color: texto blanco (text-white)
  loginButtonText: "font-bold text-white text-lg",

  // registerText:
  // • Tipografía: tamaño base (text-base) y color (text-black)
  // • Alineación: centrado (text-center)
  // • Espaciado: margen superior (mt-7)
  registerText: "text-base text-black text-center mt-7",

  // registerTextHighlight:
  // • Tipografía: fuente en negrita (font-bold)
  // • Color: texto personalizado (text-[#5CB868])
  registerTextHighlight: "font-bold text-[#5CB868]",
};
